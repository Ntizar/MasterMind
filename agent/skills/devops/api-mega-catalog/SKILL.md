---
name: api-mega-catalog
description: "Usa a buscar en el catálogo masivo de APIs públicas."
version: "2.0.0"
tags: [api, catalogo, public-apis, referencias, lista, markdown]
related_skills: [api-mega-catalog, nango, google-workspace]
---

# API Mega Catalog — catálogo masivo de APIs públicas

> ⚠️ Corrección 2026-09-05 (auditoría): el catálogo declara **11.860 APIs** (no 6.000+), y es una **lista en Markdown** — **no** hay librería JS `apiCatalog`/`findAPI`.

**Repo:** `https://github.com/cporter202/API-mega-list` (JavaScript, ~7.6K⭐) — lista markdown de APIs públicas.

## When to Use

- Cuando pidas **encontrar APIs públicas** por categoría/dominio (no es una librería importable — es un catálogo/referencia en markdown).

## Qué es

Una **lista curada en Markdown** de ~11.860 APIs públicas clasificadas. Sirve como referencia para elegir una API; no se importa como paquete.

## Uso

- Consultar el README/catálogo y filtrar por categoría.
- **No** hay `apiCatalog.findAPI()` — es un documento de referencia.

## Pitfalls

- Son **11.860 APIs**, no 6.000+.
- Es una **lista markdown**, no una librería con `apiCatalog`/`findAPI`.

## Verificación

- Abrir el catálogo y encontrar una API del dominio buscado.
