# Estado del sistema a 2026-08-28 (snapshot de la migración)

Migración completa NtizarBrainMasterMind (MicroVM Linux) → MasterMind v4.2 (Windows local). Este snapshot registra las decisiones y el estado verificado; para operación diaria ver el SKILL.md padre.

## Historia en una línea

1. Repo antiguo clonado, renombrado en GitHub a `Ntizar/MasterMind` (`gh repo rename`)
2. Limpieza: hermes-home/, hermes-backup/, legacy/, skills/ duplicada eliminados; todo fundido en `agent/`
3. Skills restaurados a la instalación Hermes local (104 añadidos, 74 ya existentes); memoria unida sin pérdidas
4. Purga de "Koldo" (nombre antiguo del agente) en repo e instalación local (era 0 referencias)
5. Gateway instalado como startup item; 3 crons creados y anclados a glm5.3-flash
6. ChromaDB embebida instalada (1.5.9, Python del sistema); 308→311 skills indexados con qwen3-embedding
7. Doctor creado (scripts/doctor.py + cron diario con autocuración segura)
8. READMEs ES/EN rediseñados desde cero, sin historial de versiones

## Verificaciones en vivo (todas reales, no supuestas)

- Consulta semántica "como montar un cron de backup automatico" → devuelve batch-cron-jobs, cron-chunked-batch, mastermind (scores 0.5-0.6) ✓
- Doctor primera pasada detectó: gateway vivo, 311=311 skills, registry fresco, git pendiente ✓
- Scout corrió solo a medianoche, procesó 3 repos, mantuvo ChromaDB sincronizado ✓

## Bugs encontrados y corregidos durante la migración

| Bug | Síntoma | Fix |
|---|---|---|
| NaN 403 en urllib | embeddings fallan con curl OK | header User-Agent custom |
| STARS_REGISTRY en formato MSYS | registry se escribe en `C:\c\Users\...` fantasma | wrapper pasa ruta `C:/Users/...` nativa |
| Python venv sin pip | `pip install` no instala nada visible | usar Python312 del sistema |
| autocrlf ruido | diffs fantasma de 3 ficheros STEM | `.gitattributes` `* -text` |

## Ideas de Gentle-AI evaluadas (no adoptar el framework)

David preguntó por github.com/Gentleman-Programming/gentle-ai. Decisión: NO instalar (en Hermes es "detect-only", duplicaría memoria/skills/routing). Ideas adoptadas o en cola:
- ✅ Doctor (health check read-only + autocura) — implementado
- ⏳ Model routing por fase (política de modelos en SOUL) — pendiente, barato
- ⏳ Quality gate del digest (auditar skills creados en la semana con skill-lifecycle) — pendiente
- ❌ SDD formal, Engram, receipts RDD — overkill para agente personal de 1 usuario

## Pendientes conocidos

- Borrar residuo `C:\c\` (lo crea el bug de rutas MSYS si algún wrapper viejo corre; requiere aprobación de David)
- Conectar bot de Telegram al gateway si David quiere delivery en vivo de crons
- Activar qwen3.8-flash cuando se acaben los tokens de glm5.3
